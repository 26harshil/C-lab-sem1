#include<stdio.h>
void main()
{
	int a,b;
	float bill;
	printf("enter the unite = ");
	scanf("%d",&a);

	if(a<=50)
	{
		printf("the bill is %f ",bill=a*0.50);
	}
		if(a<=150)
	{
		printf("the bill is %f ",bill=50*0.50 +(unite-50)*0.75);
	}
		if(a<=250)
	{
		printf("the bill is %f ",bill=50*0.50+150*0.75+);
	}
	
}
